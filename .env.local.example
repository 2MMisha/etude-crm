# Copy to .env.local (gitignored) for local development. Never commit real values.

VITE_GITHUB_OWNER=2MMisha
VITE_DATA_REPO=etude-crm-data

# The one shared PAT with contents:read/write on etude-crm-data (see README).
# This ships inside the built JS bundle — treat it as public once deployed.
VITE_GITHUB_TOKEN=
